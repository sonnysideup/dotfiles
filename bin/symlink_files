#!/usr/bin/env bash

set -e

configs=(
  gitconfig
  gitignore
  tmux.conf
  vimrc
)

for cfg in "${configs[@]}"; do
  cfg_path="$(pwd)/$cfg"
  ln -sf $cfg_path ~/.$cfg
  echo "created $cfg_path"
done
